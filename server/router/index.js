const Router = require('express').Router();
const { ReviewCtrl } = require('../controllers/index')

Router.route('/reviews')
  .get()

Router.route('/reviews')
  .get()

module.exports = {
  Router,
}